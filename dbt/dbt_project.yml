name: trackdown
version: '1.0.0'
config-version: 2

profile: trackdown

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  trackdown:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
